server:
  port: 7002

spring:
  application:
    name: ad-dashboard

eureka:
  client:
    service-url:
#      defaultZone: http://server1:8000/eureka/
      defaultZone: http://localhost:8000/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

